# PHP 常用命

```bash
#调用 phpinfo() 函数并显示出结果
php -i 
#查看加载的 PHP 模块
php -m
#单行 PHP 代码执行，无需加上 PHP 的起始和结束标识符
php -r "echo hello world"
查看PHP的配置文件名
php --ini
运行内建的web服务器，指定IP和端口
php -S localhost:8888 test.php
```
